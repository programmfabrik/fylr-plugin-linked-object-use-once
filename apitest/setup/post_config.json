[
    {
        "name": "post config",
        "request": {
            "header_from_store": {
                "Authorization": "access_token"
            },
            "endpoint": "config",
            "method": "POST",
            "body": {{ file_render `base-config-fylr.json` }}
        },
        "response": {
            "statuscode": 200
        }
    }
]