[
    {
        "name": "enable fylr-plugin-linked-object-use-once plugin",
        "request": {
            "endpoint": "plugin/manage/{{ datastore `plugin_id` }}",
            "method": "POST",
            "body": {
                "id": {{ datastore `plugin_id` }},
                "enabled": true
            }
        },
        "response": {
            "body": {
                "enabled": true
            }
        }
    }
]